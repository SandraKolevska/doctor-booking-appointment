{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "frontend" 
      }
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/frontend/index.html" }
  ]
}